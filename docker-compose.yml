version: '3.8'

services:
  springboot-app:
    image: mining-service
    build:
      context: .
      dockerfile: ./mining-service/infrastructure/Dockerfile
    ports:
      - "8080:8080"
   
    networks:
      - springboot-network

networks:
  springboot-network:
    driver: bridge